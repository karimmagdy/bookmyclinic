// This route is superseded by app/confirmation/[id]/page.tsx
// Kept as a non-routable stub to satisfy git history.
// Next.js will use [id] as the canonical dynamic segment.
export { default } from '../[id]/page';
